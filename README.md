# restaurant-management-backend

a restaurant management backend written in go.

provides a rest api for handling restaurant operations including users, menu, food, tables, orders, and invoices.

supports basic crud operations and clean separation of concerns using controllers, routes, and models.

built with gin for routing and mongodb for data persistence.
