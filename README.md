# Restaurant Management API

## Overview
Backend API for a Restaurant Management System built with **Golang**, **Gin**, and **MongoDB**.  
Currently implements the **User "Get All" endpoint** and **Swagger API documentation**.

---

## Project Structure
